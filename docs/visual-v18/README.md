# v18 视觉对比稿

日期：2026-09-05。本文描述初版视觉原型；2026-09-06 已选定 C，正式实现进度见下方。

## 正式实现（2026-09-06）

C 页面样式、四档优先级和头像图标已实现，等待用户本地验收，未部署。正式预览 http://127.0.0.1:5190 （以本地现有数据建立独立副本，未同步最新线上数据）。

62 项测试、lint、build 通过。隔离演示库浏览器验证优先级保存不会改变状态和日期、完成/重新打开级联、筛选、步骤覆盖、月视图与周报页面、图标无需登录访问。implementation/desktop.png 与 mobile.png 为本地数据副本的最终截图；detail.png 为隔离测试详情截图。

自然语言优先级仅解析用户明确指定的信息，未进行真实模型网络调用回归；解析校验与确认落库已测试。真实手机收藏和主屏幕图标需设备验收。

## 已确认范围

- 以视觉精修为主，保留现有周看板功能结构。
- 三版：A 清爽工作台、B 纸感工作日志、C Kimi 品牌工作台（推荐）。三版共用演示任务。
- 正式实现将增加四档优先级：重要且紧急、重要不紧急、紧急不重要、不紧急不重要。历史任务保留未设置；主任务优先级默认由步骤继承，允许覆盖。
- 正式实现将使用 public/avatar.png 生成收藏、浏览器标签与桌面图标。
- 用户已确认先做对比稿，选定视觉后再实施正式页面。

## 交付

index.html 可直接打开，或在项目目录启动本地静态服务后访问 /docs/visual-v18/。默认 C，顶部切换方案；支持演示项目筛选、优先级筛选、点击标签循环四档、超期事项展开。

a/b/c-desktop.png 和 a/b/c-mobile.png 为六张完整页面截图。桌面 1440px，手机 390px。手机演示将今天置顶。

所有任务与统计均为演示数据，统计不会随演示修改更新。录入只提示，不保存；月份、周报和日期箭头为视觉占位。原型仅设置头像 favicon，正式多尺寸图标与桌面适配尚未实施。

## 视觉依据

Kimi 官方品牌手册：https://www.kimi.com/resources/kimi-brand 。C 使用暖白、中性灰、#007CFF、网格与局部数字纹理。原型使用项目已有 HarmonyOS Sans SC 与 Geist Mono；Inter 尚未加入。

## 验证

本地浏览器完成六张截图；检查桌面与手机无横向溢出；方案切换、项目与优先级筛选、优先级演示修改、异常展开通过，无页面脚本异常。已检查桌面三版及手机 C；折叠区域样式冲突已修正并重新截图。

## 后续

用户选定视觉方向后，落实正式数据库/API/录入与任务详情优先级、完整图标、响应式样式；按项目标准执行测试、lint、构建和桌面/手机验收。本轮未修改生产代码或生产数据，未部署。

## 预览数据同步修正（2026-09-06 11:43）

用户明确授权下载线上完整最新快照用于验收。已通过 SQLite backup 读取一致性快照到独立本地临时目录（WORK_OS_DATA_DIR），预览端口仍为 5190。线上未修改，原本地预览副本保留。
线上与预览 163 条任务的 id/name/project_id/status/deadline/planned_date/parent_task_id/completed_at 摘要完全一致。浏览器逐日核对本周 15 条、上周 11 条日期记录，全部正确归位；截图 implementation/live-week-0.png、live-week-1.png。
这是 11:43 的线上快照，不持续自动同步。先前 22 条旧本地副本不再用于本次验收。

## 优先级滚动修复（2026-09-06）

WeekView 内嵌定义的 Card/DayColumn/ToggleBtn 在父组件更新时被重新创建，保存优先级会导致实际 DOM 重挂载。已移到模块级组件，通过 WeekContext 获取当前数据，保留组件身份与滚动锚点。未修改数据和业务规则。
验证：62 项测试、lint、build 通过；scripts/verify-priority-scroll.cjs 拦截 PATCH、无数据写入，1280px 成功滚动 440→440，390px 成功 2266→2266，原节点保留；失败时错误提示带来 28px 高度变化但无回顶。预览 5190 已重启，仍使用 11:43 线上快照及此后本地验收改动。
